VITE_API_URL=http://localhost:5000/api

# "Continue with Google" — must match GOOGLE_CLIENT_ID in backend/.env.
# This is a public OAuth Client ID (not a secret) — it's meant to be
# visible in the browser. Never put the Google *client secret* here.
VITE_GOOGLE_CLIENT_ID=
